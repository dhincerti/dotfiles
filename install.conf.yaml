- defaults:
    link:
      relink: true
      create: true

- clean: ['~']

- link:
    ~/.gitconfig:
      path: gitconfig
      force: true
    ~/.config/autostart/:
      glob: true
      force: true
      path: config/autostart/*

#- aptget:
#    - curl
#    - htop
#    - guake